import { Editor } from '@modules/editor/presentation';
import type { ReactNode } from 'react';

function EditorPage(): ReactNode {
	return <Editor />;
}

EditorPage.displayName = 'EditorPage';

export default EditorPage;
